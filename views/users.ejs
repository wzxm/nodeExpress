<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/css/style.css' />
  </head>
  <body>
    <h1><%= title %></h1>
	
    <ul>
    	<% users.forEach(function(user){ %>
    		<li><%= user.username %></li>
    	<% }) %>
    </ul>

    <p><%= username %></p>
  </body>
</html>
